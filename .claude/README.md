# claude

Claude Code configuration and skills.

**Tracking policy:**

- This directory uses an allowlist `.gitignore`; only files explicitly un-ignored are tracked
- Local plans, debug logs, and other generated state remain ignored unless explicitly allowed
