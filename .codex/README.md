# codex

OpenAI Codex configuration and rules.

**Tracking policy:**

- This directory uses an allowlist `.gitignore`; only files explicitly un-ignored are tracked
- `rules/default.rules` is machine-generated and remains ignored
